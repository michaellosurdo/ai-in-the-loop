{
  "hash": "8a729ec485fb4f7d37ce49f342fc8641",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Analyzing NBA Shot Patterns with Python\"\ndescription: \"A quick exploration of shot distances and their correlation with shooting percentage\"\nauthor: \"AI In The Loop\"\ndate: \"2024-01-02\"\ncategories: [sports, python, data-analysis, test]\n---\n\n:::{.callout-note}\nThis is an AI-generated post used for testing purposes only.\n:::\n\nIn this post, we'll explore a simple analysis of NBA shot patterns using Python. We'll create some synthetic data and analyze how shooting percentage varies with distance.\n\n::: {#cell-3 .cell execution_count=1}\n``` {.python .cell-code}\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\n\n# Set random seed for reproducibility\nnp.random.seed(42)\n\n# Generate synthetic shot data\nn_shots = 1000\ndistances = np.random.normal(15, 5, n_shots)  # Most shots between 10-20 feet\n# Shooting percentage decreases with distance\nprobabilities = 0.6 - 0.02 * distances + np.random.normal(0, 0.1, n_shots)\nmade_shots = np.random.binomial(1, np.clip(probabilities, 0, 1))\n\n# Create DataFrame\nshot_data = pd.DataFrame({\n    'distance': distances,\n    'made': made_shots\n})\n```\n:::\n\n\n::: {#cell-4 .cell execution_count=2}\n``` {.python .cell-code}\n# Calculate shooting percentage by distance range\nshot_data['distance_range'] = pd.cut(shot_data['distance'], bins=10)\nshooting_pct = shot_data.groupby('distance_range')['made'].mean()\n\n# Create visualization\nplt.figure(figsize=(10, 6))\nshooting_pct.plot(kind='bar')\nplt.title('Shooting Percentage by Distance')\nplt.xlabel('Distance Range (feet)')\nplt.ylabel('Shooting Percentage')\nplt.xticks(rotation=45)\nplt.tight_layout()\nplt.show()\n```\n\n::: {.cell-output .cell-output-stderr}\n```\n/tmp/ipykernel_237146/3137092709.py:3: FutureWarning: The default of observed=False is deprecated and will be changed to True in a future version of pandas. Pass observed=False to retain current behavior or observed=True to adopt the future default and silence this warning.\n  shooting_pct = shot_data.groupby('distance_range')['made'].mean()\n```\n:::\n\n::: {.cell-output .cell-output-display}\n![](index_files/figure-html/cell-3-output-2.png){width=944 height=566}\n:::\n:::\n\n\nAs we can see from the visualization, there's a clear negative correlation between shot distance and shooting percentage, which matches what we would expect in real basketball games.\n\n",
    "supporting": [
      "index_files"
    ],
    "filters": [],
    "includes": {}
  }
}